# 任务表 · 绮点 AI 小红书运营平台

> **纪律：同一时刻只能有一个 `[~]`。** 做完一个 → 验证一个 → 再开下一个。
> 标记：`[ ]`未开始 ｜ `[~]`进行中 ｜ `[x]`完成（须在 `06-verification.md` 有证据）｜ `[!]`阻塞
> 建立：2026-09-18 ｜ 最后更新：2026-09-18

---

## 进行中

（无 —— 下一个任务开之前，先从这里取一条）

---

## 待做（按优先级）

**P0（交付前必须）**
- [ ] T46 Mac Intel 交付适配　验收：真实 Intel Mac（或 VM）双击 `.command` 启动，浏览器自动打开 `127.0.0.1:8787` 且 `GET /api/health` 返回 200；交付包内 `grep -r "sk-"` 无结果
- [ ] T47 真实发布一次验证　验收：发布后 MCP 返回 `note_id`，且 `GET /api/contents` 中该条 `status` 变为 `published`；发布间隔记录写入 `publish_tasks`

**P1**
- [ ] T44 评论自动回复　验收：造一条命中知识库的测试评论 → 5 分钟内自动回复且长度 30–60 字；造一条未命中的 → 不回复，进人工待办；人工介入后该用户不再自动回复
- [ ] T42 复盘报告与建议　验收：`POST /api/report` 返回含"本期表现/与上期对比/≥3 条建议/支柱调整"四段；样本 <3 篇时返回"数据积累中"而不给建议

**P2**
- [ ] T43 手工填报（咨询/到店）　验收：录入当日咨询数 5、到店数 2 → 回读一致；同日再录 8/3 → 覆盖为 8/3（不新增行）
- [ ] T45 AI 生图两档　验收：档位1 出图 ≤30 秒；档位2 提示词经 DeepSeek 扩写（日志中可见扩写前后差异）；两档产物均出现在素材库

---

## 已完成（证据见 `06-verification.md`）

- [x] T11 历史内容导入　验收：拉 60 篇 → 入库 46 篇，跳过 14 篇可对账；重复导入不新增
- [x] T12 AI 内容生成引擎（四参考系 + 七天结构）　验收：2 天内容 ≤10 秒生成；D2 承接 D1 结论；用户指定人设被遵循
- [x] T13 查重门禁（四维加权）　验收：4 组正负用例全判对；历史标题 100% 拦截；比对 46 篇
- [x] T14 文案编辑与审核流转　验收：改标题保存后接口回读已变；状态 `draft→approved`
- [x] T15 定时发布 + 建议发布时间　验收：排期未来时间任务为 `pending` 且不立即发布；`best-time` 返回 `recommended` + 理由
- [x] T16 发布状态机与预检　验收：跑一次调度后未来任务仍 `pending`（不误发）；预检 5 项输出齐全且无配图时 `pass=false`
- [x] T17 平台数据接入（浏览量/涨粉/曝光/时长）　验收：近30天 浏览量 119(+296%)、涨粉 1、曝光 28(+460%)、观看 852 秒；趋势 30 天
- [x] T18 行业热榜抓取　验收：抓"底妆" → 20 条全入库；榜单 21 条；封面 8/8 加载成功
- [x] T19 运营计划表（定位 + 支柱 + 每天条数）　验收：写"每天 2 条" → 回读 2 条；页面显示 7×2=14 篇
- [x] T20 素材库　验收：`GET /api/assets` 返回 200；页面只有一套网格（无演示假数据并存）
- [x] T21 单服务托管改造（后端直出前端）　验收：`http://127.0.0.1:8787` 页面 HTTP 200；5199 转发亦通
- [x] T41 对标账号监控　验收：`discover` 返回 8 个候选（5–8 区间），阈值自动 3→2 并回传；页面渲染 6 维度；数据不足如实标注　证据见 06-verification.md
- [x] T30 建立 SDD 标准并补齐规格文档　验收：`validate_sdd.py xhs-ops-platform` 输出 **10/10、0 严重、0 警告**　证据见 06-verification.md
- [x] T22 图片防盗链代理　验收：热榜 8/8 张封面 `naturalWidth>0`（不再 403）

---

## 阻塞

- [!] T48 前端"未做功能"按钮的假动画　验收：点"重新生成 7 天草稿""一键发布"等按钮，要么执行真实操作，要么给出明确"功能未开放"提示，**不得播放假动画**　阻塞原因：**等用户拍板**（改成真调用 or 先隐藏该按钮）

---

## 进度总览

| 状态 | 数量 | 对应需求 |
|---|---|---|
| ✅ 已完成 | 14 | R1–R11 + 架构两项 + SDD 治理 |
| 🔄 进行中 | 0 | — |
| ⬜ 待做 | 6 | R12–R16 + 真实发布验证 |
| ⛔ 阻塞 | 1 | 等用户决策 |
