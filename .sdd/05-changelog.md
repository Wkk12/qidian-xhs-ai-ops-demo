# 规格变更日志 · 绮点 AI 小红书运营平台

> 规格每改一次，这里加一行。**代码改了规格必同步改。**
> 格式：日期 | 变更 | 原因 | 影响范围
> 建立：2026-09-18（回填历史）｜ 最后更新：2026-09-18

| 日期 | 变更 | 原因 | 影响 |
|---|---|---|---|
| 2026-09-18 | 修复 R15 生图产物 URL 前缀 `/uploads/` → `/files/`（静态服务前缀不一致致 404） | 8 模块走查发现生成的图在页面显示不出来 | 02-plan、01-spec R15 |
| 2026-09-18 | `/api/img` 上游失败改为回透明占位图（原直接 502 破图） | 小红书图片 URL 带过期时间戳，过期后必然取不到 | 02-plan |
| 2026-09-18 | **R17：清除前端假按钮与失效引用**（新增验收项） | 审计发现「生成 7 天」按钮仍接桩子函数；`runGenerate` 写错变量；模板引用已不存在的 `imgGenResult` | 01-spec R17 |
| 2026-09-18 | **R16：Mac Intel 适配完成，解除「唯一硬骨头」** | 实测 `CGO_ENABLED=0` 交叉编译 29 秒成功，产出 amd64+arm64 双 Mach-O；源码经查零 CGO、零硬编码 Windows 路径、依赖全纯 JS | 01-spec R16、07 部署文档 |
| 2026-09-18 | 交付包新增 0755 执行位写入 | Windows 无法设 unix 权限位，改在 zip 元数据 `external_attr` 写入，用户解压即双击可用 | 01-spec R16 |
| 2026-09-18 | **R15 标准档耗时口径由 ≤30 秒改为 ≤90 秒** | 实测 54.5 秒（扩写 1.2s + 渠道 53.3s），30 秒对第三方中转渠道不现实；瓶颈不在本系统 | 01-spec R15 |
| 2026-09-18 | R15 明确「扩写单独可预览」与「渠道错误不静默降级」 | 扩写只花 1.2 秒可单独预览省钱；渠道 404/无渠道须明确报错 | 01-spec R15 |
| 2026-09-18 | R14 评论回复落地：新增 `comments.js` + 通知封装 + `human_takeover` 表 | 需求实现 | 02-plan、01-spec |
| 2026-09-18 | `comments` 表新增 `note_title` / `user_id` / `skip_reason` 三列 | 回复决策需要记录用户与跳过原因 | 02-plan 数据模型 |
| 2026-09-18 | 修复 `/api/knowledge` 传数组时 500（node:sqlite 不接受数组绑定） | 实测灌知识库时暴露 | 02-plan |
| 2026-09-18 | R12 判据增加「收藏全为 0 时退回平均点赞 + 显式告警」 | 实测本账号 60 篇收藏数全为 0（真实数据），收藏率恒为 0，原判据失效 | 01-spec R12 |
| 2026-09-18 | R12 支柱调整增加默认兜底 + 来源标注；AI 失败降级不整体失败 | positioning 未配置时该段会空 → 用与 /api/positioning 相同的默认支柱兜底 | 01-spec R12 |
| 2026-09-18 | 新增 `snapshots` 表（24/48/72h 快照） | R12 采集时点要求 | 02-plan 数据模型 |
| 2026-09-18 | R11 阈值行为改为「默认 3，候选<5 时自动放宽至 2 并回传实际阈值」 | 实测热榜 136 条中最高仅出现 3 次，达标作者只有 2 个，达不到 5–8 个 | 01-spec R11 |
| 2026-09-18 | R11 新增「深度分析」动作与实现约束 3 条 | 实测发现：MCP 搜索结果无发布时间，只在笔记详情 `note.time`；且详情需 `xsec_token`、13 秒/条、有限流 | 01-spec R11、02-plan |
| 2026-09-18 | `trends` 表新增 `xsec_token` 字段 | 拉笔记详情的必需参数 | 02-plan 数据模型 |
| 2026-09-18 | **建立 SDD 文档体系**（宪法/规格/方案/任务/决策/变更/验证 七文件） | 项目审计不符合 SDD，需迁移到标准 | 全部 |
| 2026-09-18 | 新增 R16「Mac Intel 交付适配」并定为 P0 | 客户机为 Mac Intel，交付前必做 | 01-spec、03-tasks |
| 2026-09-18 | R3 验收标准新增「历史笔记无正文时退回标题单维判定」 | 实测发现标题抄袭拦不住（仅 30%） | 01-spec、02-plan |
| 2026-09-18 | R6 新增「距上次发布 <30 分钟 → 预检拦截」 | 防风控 | 01-spec、02-plan |
| 2026-09-18 | R8 新增「抓取间隔 ≥3.5 秒」 | 实测更快会触发平台限流 | 01-spec、02-plan |
| 2026-09-18 | 不做清单新增 10 条（含私信、多账号、Obsidian 等） | 明确范围边界，防止擅自加戏 | 01-spec |
| 2026-09-18 | 红线新增第 1、6 条（禁止技术栈外依赖 / 禁止擅自扩范围） | 审计发现 16 个接口为擅自新增 | 00-constitution |
| 2026-09-17 | R7 数据源从"MCP"改为"MCP + 创作者中心 API" | MCP 无浏览量/涨粉字段，实测创作者中心可直调 | 01-spec、02-plan |
| 2026-09-17 | R2 确定七天结构为「每次只做 7 天量 + 可选每天条数 + 之后每天补 1 天」 | 用户两次修正后的最终理解 | 01-spec、02-plan |
| 2026-09-17 | 新增 R15「AI 生图两档」 | 用户需求 | 01-spec |
| 2026-09-17 | 新增 R14「评论自动回复」（规则监控 + AI 生成） | 用户澄清"不让 AI 做监控" | 01-spec |
| 2026-09-17 | 红线新增第 8 条（生图不得直用原始提示词 / 小号中文用 Pillow） | 实测小号中文必错 | 00-constitution |
| 2026-09-16 | 新增 R10「素材库」并把"在线素材"定义为上传位 | 用户澄清"在线素材=上传和生成图的位置" | 01-spec |
| 2026-09-16 | 交付边界新增「内部/对外文档分离」 | 客户版不得出现 agent/MCP/DeepSeek 等技术内幕 | 00-constitution、02-plan |
| 2026-09-15 | 确定架构为单服务托管（后端直出前端） | 前端 dev server 掉线导致用户打不开页面 | 02-plan（ADR-004）|
| 2026-09-15 | 红线新增第 7 条（只用 BiRefNet 抠图） | 用户明确要求 | 00-constitution |
| 2026-09-14 | R9「运营计划表」定为生成依据的默认来源 | 用户要求"内容跟运营计划表走" | 01-spec |
| 2026-09-14 | 确定四参考系权重 0.45/0.25/0.20/0.10 | 用户要求"用户输入权重更高" | 01-spec、02-plan |
| 2026-09 上旬 | 项目启动：确立 Node 单栈 + 不装 agent + 前端定稿不改 | 交付约束 | 00-constitution（ADR-001/003）|
